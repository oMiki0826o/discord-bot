# firefly-bot 依賴套件
# 使用 pip install -r requirements.txt 安裝

# ── Discord ──────────────────────
discord.py==2.7.1

# ── Google AI ──────────────────────
google-genai==2.5.0
google-auth==2.53.0

# ── 環境變數 ──────────────────────
python-dotenv==1.2.2

# ── 音樂播放 ──────────────────────
yt-dlp>=2024.1.1
PyNaCl>=1.5.0

# ── HTTP ──────────────────────
aiohttp==3.14.0
httpx==0.28.1

# ── 文件解析（file_parser）──────────────────────
# 修正：原本 pypdf2（安裝後模組名稱是 PyPDF2）與程式碼實際 import 的
# pypdf 是兩個不同套件，導致 PDF 解析一直靜默失敗。改用 markitdown
# 統一處理 pdf / docx / xlsx / xls / pptx，需要的底層套件（pdfminer.six /
# python-docx / openpyxl / python-pptx 等）由 markitdown 依 extra 自動安裝，
# 不需要再各自於此手動列出版本。
markitdown[pdf,docx,pptx,xlsx,xls]>=0.1.6
mutagen>=1.47.0
pymediainfo>=6.0.0

# ── 型別 ──────────────────────
typing_extensions==4.15.0
pydantic==2.13.4

# ── 工具 ──────────────────────
tenacity==9.1.4
